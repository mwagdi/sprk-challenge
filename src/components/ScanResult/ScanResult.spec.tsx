describe('ScanResult', () => {
    describe('Product not found', () => {
        it('should show GTIN', () => {
        });
        it('should show `Product not found`', () => {
        });
    });

    describe('Product found', () => {
        it('should show GTIN', () => {
        });
        it('should show Link to product page', () => {
        });
    });
})